<div class="container">
  <%= tag.h1 "投稿一覧" %>
    <table class="table">
      <thead>
        <%= tag.tr %>
          <%= tag.th "タイトル" %>
          <%= tag.th "本文" %>
      </thead>
      <tbody>
        <% @post.each do |post| %>
          <%= tag.tr %>          
            <%= tag.td post.title %>
            <%= tag.td post.content %>
        <%end%>
      </tbody>
    </table>
</div>