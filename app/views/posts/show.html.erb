<div class="container">
  <%= tag.h1 "#{@post.user.name}の投稿" %>
  <%= tag.p "タイトル：  #{@post.title}" %>
  <%= tag.p "投稿内容：  #{@post.content}" %>
  <%= link_to("編集", post_edit_path(@post.id), class:"btn btn-primary") %>
</div>